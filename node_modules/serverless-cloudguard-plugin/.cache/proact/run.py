import os
import sys
os.environ["PROTEGO_ROOT_DIRECTORY"] = os.path.join(os.path.abspath(os.path.dirname(__file__)), "cli_functions_scanner")
# FUNCTION_SCANNER environment variable used to indicate logger not to add fcid and flow for scanner log statements
os.environ["FUNCTION_SCANNER"] = "TRUE"

try:
    sys.path.remove("/home/usr")
except ValueError:
    pass

from cli_functions_scanner import functions_scanner
functions_scanner.main()
